# This package is used to enable password authentication in sshd_config file
# Make sure to handle the package as root user

# Step 1 - Unzip the PassAuth.zip file

# Step 2 - Run the script "applyPassAuth.sh"
          ./applyPassAuth.sh
          
# Step 3 - After successful changes, start using "passAuth.sh" script
          to enable or disable Password Authentication
          ./passAuth.sh
